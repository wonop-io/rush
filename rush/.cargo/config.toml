[build]
rustflags = ["--cfg", "tokio_unstable"]

[alias]
# Run CI checks: formatting, sorting, autoinherit, and clippy
# This creates a cargo alias that runs a script
run-ci = ["run", "--package", "xtask", "--", "ci"]
