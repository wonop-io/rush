backend:
  RUST_LOG: !Static "debug"  # Less verbose for local development
  DEBUG_MODE: !Static "true"  # Additional local-only variable

frontend:
  NODE_ENV: !Static "local"  # Override development with local